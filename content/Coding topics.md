## TDD

```dataview
TABLE title, status, date
FROM "content/topics" and #tdd 
WHERE status = "active"
SORT date desc
```

## Clean Code

```dataview
TABLE title, status, date
FROM "content/topics" and #cleancode 
WHERE status = "active"
SORT date desc
```